import { StyleSheet } from 'react-native';

const ICON_SIZE = 20;
const styles = StyleSheet.create({
  imageContainer: {
    marginLeft: 10
  },
  image: {
    height: ICON_SIZE,
    width: ICON_SIZE
  }
});

export default styles;
